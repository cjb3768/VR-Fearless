using UnityEngine;
using System.Collections;

public class ChangeSize : MonoBehaviour {

	//Vector3 start_size = transform.position;

	// Use this for initialization
	void Start () {
		//Transform.position = start_size;

	}

	// Update is called once per frame
	void Update () {
		//GameObject cube = GameObject.Find ("Cube");
		//Vector3 myPosition = transform.position;
		//float scaleDir = Input.GetAxis ("Fire2");
		//Vector3 speed = new Vector3 (0.0f, 0.0f, 0.0f);
		//if(Transform.position.x == 0.0f) {
		//	Transform.localPosition.z += scaleDir * 0.1f;
		//}
		//if (Transform.position.z == 0.0f) {
		//	Transform.localPosition.x += scaleDir * 0.1f;
		//}
	
		//transform.position = myPosition;
	}
}
